spring.application.name=audio-streamer
# Dynamic port binding for Render deployment (uses PORT env var if available, defaults to 8080)
server.port=${PORT:8080}
spring.main.web-application-type=reactive

# Gemini API Configuration
gemini.api.key=${GEMINI_API_KEY:}

